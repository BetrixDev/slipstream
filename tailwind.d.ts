declare module "tailwindcss/lib/util/flattenColorPalette" {
  // You can specify more precise types if you know them
  // biome-ignore lint/suspicious/noExplicitAny: this isn't being used anywhere except for tailwind config so type doesn't matter a whole lot
  const flattenColorPalette: any;
  export default flattenColorPalette;
}
